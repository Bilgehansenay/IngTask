# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Ing Task' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Ing Task
  pod 'Moya', '~> 14.0'
  pod 'Moya-ModelMapper', '~> 10.0'
  pod 'Kingfisher', '~> 4.0'
  pod 'IQKeyboardManagerSwift' # Custom Keyboard
  pod 'RealmSwift' # ORM DB

  
  target 'Ing TaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Ing TaskUITests' do
    # Pods for testing
  end

end
